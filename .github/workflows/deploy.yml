name: Generar y publicar mapa interactivo

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4  # Esta versión ya es compatible con Node.js 24

      - name: Configurar Python
        uses: actions/setup-python@v5  # Esta versión ya es compatible con Node.js 24

      - name: Instalar dependencias
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Ejecutar script (genera index.html)
        run: python mapa_clustering_ecuador.py

      - name: Publicar en GitHub Pages
        uses: peaceiris/actions-gh-pages@v4  # ¡Cambiamos de v3 a v4!
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./
          publish_branch: gh-pages
          keep_files: false
          destination_dir: ./
